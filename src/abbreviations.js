"use strict";

import { HELP_OPTION,
         VERSION_OPTION,
         FAIL_FAST_OPTION } from "./options";

const h = HELP_OPTION,
      v = VERSION_OPTION,
      f = FAIL_FAST_OPTION;

const abbreviations = {
  h,
  v,
  f
};

export default abbreviations;
